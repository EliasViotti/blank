{
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "/usr/bin/supervisord",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300,
    "restartPolicyType": "always"
  }
}
